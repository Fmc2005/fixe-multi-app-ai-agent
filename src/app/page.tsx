// Placeholder. Frontend (Connect / Know you / Landing plan / Execution)
// comes after the backend endpoints below are wired to live integrations —
// see CLAUDE.md "Frontend".
export default function Home() {
  return (
    <main>
      <h1>Landing</h1>
      <p>
        Backend scaffold in progress. Endpoints: <code>POST /api/plan</code>,{" "}
        <code>POST /api/execute</code>.
      </p>
    </main>
  );
}
